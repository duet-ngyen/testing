<header class="navbar navbar-fixed-top navbar-inverse">
  <% if user_signed_in? %>
    <% if current_user.teacher? %>
      <%= link_to "Create new question", new_teacher_question_path, class:"btn btn-default" %>
      <%= link_to "Create examination", new_teacher_examination_path, class: "btn btn-primary" %>
    <% else %>
    <%= link_to "Choose examination", examinations_path, class:"btn btn-default" %>
    <% end %>
    <%= link_to t("header.logout"), destroy_user_session_path, method: :delete, class: "btn btn-danger"  %>
  <% else %>
    <%= link_to "Sign In", user_session_path, class:"btn btn-info" %>
  <% end %>
</header>
